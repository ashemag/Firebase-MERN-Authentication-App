import { black, darkestGray, green, white } from "./colors";
import styled from "styled-components";

export const H1 = styled.p({
  fontSize: "30px",
  fontWeight: 200,
  letterSpacing: "2px",
});

export const Body = styled.p({
  fontWeight: 200,
});

export const FooterText = styled.p({
  fontSize: "12px",
  fontWeight: 200,
  letterSpacing: 1.5,
});
